@model IEnumerable<QualitaCorpWeb.Entities.Models.Mesero>

@{
    ViewData["Title"] = "Meseros";
}

<h1>Maestro de Meseros</h1>

<p>
    <a asp-action="Create">Crear nuevo mesero</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellidos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Edad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Antiguedad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Edad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Antiguedad)
            </td>
           <!-- <td>
                <a asp-action="Edit" asp-route-id="@item.Idmesero">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idmesero">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idmesero">Delete</a>
            </td>-->
        </tr>
}
    </tbody>
</table>
